[pytest]
# Make the project root importable so "import main" works in tests
pythonpath = .

# Tell pytest where your tests live
testpaths = tests

# Nice defaults + coverage report
addopts = -q --maxfail=1 --disable-warnings --cov=main --cov=auth_logic --cov=sales_logic --cov-report=term-missing
